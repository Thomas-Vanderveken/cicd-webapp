services:
  webapp:
    build: .
    image: crazyrhino/webapp
    ports:
      - "3000:3000"
    depends_on:
      - database
    environment:
      MONGO_URL: mongodb://database:27017/webapp
  database:
    image: mongo:latest
    volumes:
      - mongo-vol:/data/db
  test:
    image: crazyrhino/webapp-test
    depends_on:
      - database
    command: ["yarn", "test", "test.spec.js"]
    environment:
      MONGO_URL: mongodb://database:27017/webapp
volumes:
  mongo-vol:
